import { LoginCredentials } from './login-credentialsmodel';

describe('LoginCredentials', () => {
  it('should create an instance', () => {
    expect(new LoginCredentials()).toBeTruthy();
  });
});
